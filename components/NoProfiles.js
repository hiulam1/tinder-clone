import { View, Text } from "react-native";
import React from "react";

const NoProfiles = () => {
  return (
    <View className="flex-1 justify-center items-center ">
      <Text className="text-4xl">No Profiles for now :(</Text>
    </View>
  );
};

export default NoProfiles;
